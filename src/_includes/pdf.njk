{% set description = 'Apaixonado por computação, curso Ciência da Computação no IFG Câmpus Anápolis. Sou Desenvolvedor Júnior,
        já estagiei por 6 meses na área e desenvolvi alguns software para uso do IFG.' %}

<div id='pdf'
     class='page-viewer w-full {{ "shadow-lg rounded-lg" if web }} bg-white flex flex-wrap {{ "md:h-a4" if web else "h-a4" }}'>
  <div class='page-content {{ "w-full md:w-8/12" if web else "w-8/12" }} py-12 px-12'>
    <div class='content-header flex items-center mb-4'>
      <img src='https://github.com/devbaraus.png' alt='Foto Bruno de Araújo Alves'
           class='h-23 w-20 rounded-full content-thumb mr-4'>
      <div>
        <h1 class='text-3xl font-semibold mb-2'>Bruno de Araújo Alves</h1>
        <p class='uppercase text-sm text-gray-500'>Desenvolvedor de Software</p>
      </div>
    </div>
    <div class='content-profile mb-4'>
      <h2 class='text-2xl font-semibold mb-2'>Perfil</h2>
      <p>Apaixonado por computação, curso Ciência da Computação no IFG Câmpus Anápolis. Sou Desenvolvedor Júnior,
        já estagiei por 6 meses na área e desenvolvi alguns software para uso do IFG.</p>
    </div>
    <div class='content-education mb-6'>
      <h2 class='text-2xl font-semibold mb-4'>Formação Acadêmica</h2>
      {% for item in education %}
        <div class='mb-4'>
          <p class='font-semibold'>{{ item.degree }}
            , {{ item.school }}{{ ', ' if item.city }}{{ item.city }}</p>
          <p
            class='text-sm text-gray-500 uppercase'>{{ item.start_date }}{{ ' - ' if item.end_date }}{{ item.end_date }}</p>
          {% if item.description %}
            <p>{{ item.description }}</p>
          {% endif %}
        </div>
      {% endfor %}
    </div>
    <div class='content-employment mb-6'>
      <h2 class='text-2xl font-semibold mb-4'>Experiência Profissional</h2>
      {% for item in employment %}
        <div class='mb-4'>
          <p class='font-semibold'>{{ item.job_title }}
            , {{ item.employer }}{{ ', ' if item.city }}{{ item.city }}</p>
          <p
            class='text-sm text-gray-500 uppercase'>{{ item.start_date }}{{ ' - ' if item.end_date }}{{ item.end_date }}</p>
          {% if item.description %}
            <p>{{ item.description }}</p>
          {% endif %}
        </div>
      {% endfor %}
    </div>
    <div class='content-internships mb-6'>
      <h2 class='text-2xl font-semibold mb-4'>Estágios</h2>
      {% for item in internships %}
        <div class='mb-4'>
          <p class='font-semibold'>{{ item.job_title }}
            , {{ item.employer }}{{ ', ' if item.city }}{{ item.city }}</p>
          <p
            class='text-sm text-gray-500 uppercase'>{{ item.start_date }}{{ ' - ' if item.end_date }}{{ item.end_date }}</p>
          {% if item.description %}
            <p>{{ item.description }}</p>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>
  <div
    class='page-info {{ "w-full md:w-4/12" if web else "w-4/12" }} bg-gray-900 py-12 px-12 md:pt-36 text-gray-100 text-sm {{ "md:rounded-tr-lg md:rounded-br-lg" if web }} '>
    <div class='info-personal mb-6'>
      <h3 class='text-xl mb-4'>Informações</h3>
      <p class='mb-2'>Anápolis, Goiás</p>
      <p class='mb-2'><a href='tel:{{ personal.phone }}'>{{ personal.phone }}</a></p>
      <p class='mb-2'><a href='mailto:{{ personal.email }}'>{{ personal.email }}</a></p>
    </div>
    <div class='info-socials mb-6'>
      <h3 class='text-xl mb-4'>Redes Sociais</h3>
      {% for item in personal.socials %}
        <p class='underline mb-2'><a href='{{ item.link }}' target='_blank'
                                     rel='noreferrer'>{{ item.label }}</a></p>
      {% endfor %}
    </div>
    <div class='info-works mb-6'>
      <h3 class='text-xl mb-4'>Trabalhos</h3>
      {% for item in personal.works %}
        <p class='underline mb-2'><a href='{{ item.link }}' target='_blank'
                                     rel='noreferrer'>{{ item.label }}</a></p>
      {% endfor %}
    </div>
    <div class='info-competences mb-6'>
      <h3 class='text-xl mb-4'>Competências</h3>
      {% for item in personal.competences %}
        <p class='mb-2'>{{ item }}</p>
      {% endfor %}
    </div>
  </div>
</div>
